# 📦 Как поднять Tavily MCP Server локально

Официальный MCP сервер **упакован в Docker-образ `mcp/tavily`**, и его можно запустить так:

## 1) Получи API-ключ Tavily

Зарегистрируйся на Tavily и возьми ключ API (он понадобится контейнеру) ([подробнее tavily.com](https://www.tavily.com/)).

## 2) Запуск Docker-контейнера через docker-compose

1. Скопируй `.env.example` в `.env` и добавь свой ключ:

```bash
cp .env.example .env
# Отредактируй .env и добавь TAVILY_API_KEY
```

2. Запусти сервер:

```bash
docker-compose up -d
```

- Сервер будет доступен на порту **8100** (можно изменить в docker-compose.yml)
- Автоматическая перезагрузка при ошибках

## 🧪 Проверка работы сервера

Для проверки работы сервера можно использовать Python скрипт. Пример успешного выполнения смотрите в [документе с примерами](successful_test.md).

### Способ: Через Python

#### Разворачиваем окружение Python через uv

```
uv sync
```

Запусти тестовый скрипт:

```bash
uv run python test_mcp_docker.py
```

Этот скрипт:
- Проверяет, запущен ли контейнер
- Тестирует инициализацию MCP соединения
- Получает список доступных инструментов
- Выполняет тестовый поиск

## 🛠 Интеграция с твоим агентом

После запуска у тебя будет сервер, который принимает MCP-запросы и возвращает результаты поиска через `tavily-search` (и другие инструменты). Подробнее об интеграции и доступных инструментах читайте в [основном README.md](../README.md).

В твоём агенте при настройке `TAVILY_URL` можно указать:

```
http://host.docker.internal:8100
```

→ Если агент под Docker IAM тестируешь локально, или

```
http://localhost:8100
```

→ Если делаешь прямой запрос из хоста (и порт проброшен).

Затем агент может вызывать этот сервер в `tools.call_tavily(...)`.

## 📝 Замечания

* MCP сервер Tavily включает несколько инструментов:
* Подробнее о проекте и его структуре читайте в [основном README.md](../README.md).

## 🐛 Поддержка

Если возникли проблемы, проверьте:
1. Доступность порта 8100
2. Корректность TAVILY_API_KEY
3. Работоспособность Docker

Для подробной информации смотрите [документацию](../README.md) и [инструкцию по запуску](run_and_testing.md).

## ❓ Возможные проблемы

1. **Контейнер не запущен**
   - Решение: `docker-compose up -d`

2. **Порт 8100 занят**
   - Решение: Измени порт в `docker-compose.yml`

3. **Нет доступа к Tavily API**
   - Проверь `TAVILY_API_KEY` в `.env` файле

4. **Сервер не отвечает**
   - Проверь логи: `docker-compose logs -f tavily`

5. **Ошибка инициализации MCP**
   - Убедись, что используешь правильный формат запроса
   - Проверь версию протокола: "2024-11-05"

## 📚 Ссылки

- [Tavily MCP Server | Docker MCP Catalog](https://hub.docker.com/mcp/server/tavily/manual)
- [Tavily - The Web Access Layer for AI Agents](https://www.tavily.com/)
- [Tavily MCP Server | Docker MCP Catalog](https://hub.docker.com/mcp/server/tavily/overview)

## 📖 Дополнительная документация

- [README.md](../README.md) - основное описание проекта и быстрый старт
- [Пример успешного тестирования](successful_test.md) - как выглядит успешный запуск тестов